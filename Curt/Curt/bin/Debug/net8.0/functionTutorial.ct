make greeting = ask("Enter a greeting >>> ")
make name = ask("Enter your name >>> ")

func sayHello(greeting, value) {
	show(greeting + " " + value)
	make name = "not the value you entered"
	show("name value inside func  = " + name)
}

call sayHello(greeting, name)

show("name value outside of function = " + name)